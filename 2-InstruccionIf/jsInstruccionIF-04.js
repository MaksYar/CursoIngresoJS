function mostrar()
{
	let vEdad;
	vEdad = parseInt(document.getElementById("txtIdEdad").value);

	if((vEdad >= 13) && (vEdad <= 17))
	{
		alert("Adolescente");
	}
	/*
	if (vEdad >= 13)
	if (vEdad <= 17)
	{
		alert("Adolescente");
	}
	*/


}//FIN DE LA FUNCIÓN